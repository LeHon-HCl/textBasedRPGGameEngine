# 03 表达式语言与求值器

| 项 | 内容 |
|---|---|
| 设计依据 | detail-design §2.3（DD-01，OQ-01 已裁决）、§3.2 |
| 需求映射 | FR-NARR-02/03、FR-XPLR-04、NFR-19 |
| 前置模块 | 01 |
| 里程碑 | M0 |

> 目标：受限 DSL 全链路——文法解析、编译校验、AST 解释执行；禁 eval/new Function。

## 任务清单

### A. 解析与编译
- [x] jsep 定制（关闭原生成员/标识符插件，加 path/call）+ EBNF 覆盖用例（§2.3 文法逐条）
- [x] `compileExpr`：AST 生成 + `refs` 抽取（VarRef 列表）
- [x] 静态校验：未知 root、未知函数、参数个数、`pure=false` 误用（EXPR_COMPILE 定位信息）

### B. 求值器
- [x] AST 解释器：算术/比较/逻辑（短路）/三元/一元/优先级
- [x] 变量域白名单映射（§2.3 表格逐项）+ 已知 root 缺 key 严格报错
- [x] 内置 20 函数（DD-01 表格逐个，2026-09-12 勘误计数）：随机类接注入 Rng，查询类读状态视图
- [x] 严格错误语义：除零/类型不匹配/嵌套隐式转换禁止（EVAL_ERROR + 表达式原文定位）
- [x] 顶层条件真值化规则（undefined/null/0/'' 为假，仅限 if/show_if/require）

### C. 测试
- [x] 表驱动测试矩阵：每运算符 × 优先级 × 短路 × 三元
- [x] 函数清单逐个用例（固定种子断言随机序列）
- [x] 错误用例全集（编译期 4 类 + 运行期 3 类，断言 ErrCode 与 where）
- [x] 性能烟测：单表达式求值 < 0.1ms（供 10 号模块 16ms 预算推算）

## 完成定义
- [x] 全部子任务勾选，覆盖率 ≥ 90%；无任何 `eval` / `new Function`（lint 扫描通过）
