# 07 文本解析与本地化运行时

| 项 | 内容 |
|---|---|
| 设计依据 | detail-design §4.1 |
| 需求映射 | FR-L10N-01/03/04/05/06、D4 |
| 前置模块 | 03（select 表达式编译）、06（locales 来源） |
| 里程碑 | M0 |

> 目标：文本键 → 当前语言 → 插值 → ResolvedText 的唯一入口；回退链与告警。

## 任务清单

- [x] `TextResolver.resolve`：键查找 + 回退主语言 + `fallbackUsed` 标记 + 主语言也缺时显示原始键并告警
- [x] 插值 `{path}`（取自 InterpVars，不做插值内表达式求值）+ 格式化 `{path|fmt:number:1}`
- [x] 复数结构（plural: one/other）与 select 变体（expr 加载期编译、resolve 期求值）
- [x] 非主语言命名空间懒加载对接（性能 NFR-01 配套）
- [x] 语言运行时切换（FR-L10N-05）：切换后 resolve 即时生效（无缓存残留）测试
- [x] 词典夹具表驱动测试：插值/复数/select/回退链/格式化/缺失告警全路径
- [x] 完成度统计函数（缺失/未使用键清单，供 26 号翻译管理复用）

## 完成定义
- [x] 全部子任务勾选，纯函数测试全绿（不依赖运行时其他模块）
