# AGENTS.md

本文件约束所有在此仓库中工作的 AI 编码代理与人类协作者。改动前请先阅读并遵守。

## 注意事项

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

## 补充约定

- 提交信息使用 Conventional Commits 格式（如 `docs:`、`feat:`、`fix:`、`test:`、`chore:`），并在正文说明动机与影响范围。
- 当前仓库处于需求基线阶段（见 `docs/proposal.md`）；进入实现期后，需求变更须先更新该文档并单独提交，再落实现代码。
